services:
  configarr:
    image: ghcr.io/raydak-labs/configarr:latest
    networks:
      - configarr
    volumes:
      - ./config:/app/config
      - ./tmp/dockerrepos:/app/repos
      - ./cfs:/app/cfs
      - ./templates:/app/templates

networks:
  configarr:
    name: configarr
    external: true
